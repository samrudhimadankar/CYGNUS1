{
  "name": "interstellar-trade-route-simulator",
  "version": "1.0.0",
  "description": "A web application for simulating trade routes between fictional star systems",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["space", "trade", "simulation", "react", "nodejs", "mongodb"],
  "author": "Interstellar Trade Simulator Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
